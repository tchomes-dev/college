tony_choma@student.uml.edu

Running the program:
1) Compile linker.c using the command below (only run this command if you make changes
	to linker.c otherwise do it once):
 	gcc -o linker linker.c
2) Run the executable file using the command below (run this every time you want to run
	your program):
 	./mic1 prom_mrd.dat program.exe 0 1024

